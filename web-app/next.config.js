/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed deprecated experimental.appDir (App Router is default in Next 13+ / 15)
  reactStrictMode: true,
}

module.exports = nextConfig
